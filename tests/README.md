# اختبارات تبارك جيم

```
npm install
npm test                 # كل المجموعات
node tests/run.js المال  # مجموعة واحدة (بحث بجزء من الاسم)
TG_JSON=out.json npm test  # حفظ النتائج بصيغة JSON
npm run test:paths       # قواعد المسارات في طرف الصدأ وحدها (على أي نظام)
node scripts/verify-build.mjs        # بوّابات البناء (المصدر + الواجهة المولَّدة)
```

الاختبارات تفتح الملف في متصفح حقيقي (Chromium عبر Playwright) وتعمل فوق
`window.TG` وحده — لا فوق تفاصيل الواجهة. لكل مجموعة سياق تخزين نظيف، فلا
تتسرّب بيانات مجموعة إلى أخرى.

المسار التنفيذي للمتصفح يُقرأ من `TG_CHROME` إن اختلف عن الافتراضي
(`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`). وإن لم يوجد أيٌّ
منهما استُعمل متصفّح Playwright نفسه، فتعمل المجموعة على أي جهاز بلا ضبط.

## المجموعات

| المجموعة | ما تثبته |
|---|---|
| ثوابت المال | كل دفعة تُنشئ سطر إيراد واحداً، المقبوض ≤ المستحق، الطباعة لا تصنع مالاً، حذف الدفعة يُبطل وصلها، المتوقّع في الدرج = الافتتاحي + الداخل − الخارج، والمصروف غير النقدي لا يُخصم منه |
| ثوابت المخزون | الرصيد = مجموع الحركات، لا رصيد سالب، وإلغاء الفاتورة يعيد الكمية |
| ثوابت الاشتراك | النهاية = النهاية الأصلية + أيام الأحداث، بعد الإنشاء والتجميد والتمديد والتعديل وإلغاء الحدث |
| الترقية v3/v4/v5/v6 ⇐ v7 | الترقية على نسخة مستعادة من إصدار أقدم لا تغيّر مبلغاً ولا تحرّك نهاية اشتراك، وتشغيلها مرتين يعطي النتيجة نفسها. وv7 تحديداً: كل حركة رأس مال تحمل طريقة (والقديمة «غير معروفة» لا مخمَّنة)، وتوزيع الشهر الواحد يُفصَّل بالضبط، والممتدّ القديم لا يُخترع له تفصيل |
| نموذج الحصص | القالب يولّد مواعيد، والحضور يحمل معرّف الموعد ويُعلّم الحجز، ولا التحاق ثابت بقي |
| العملة والجداول | العملة تُقفل بعد أول حركة مالية، وتغيير تسمية طريقة الدفع لا يمسّ سجلاً |
| البيانات التجريبية | تُحمَّل وتُحذف بالكامل من كل الجداول |
| الاستمرارية | الكتابة تنجو من إعادة التحميل (ومعها الملاحظات والتوزيعات والفترات المقفلة والشاشة الافتتاحية)، والنظام يقلع على التخزين المحلي ثم على الذاكرة مع تحذيرها |
| النماذج والشاشات المنبثقة | نماذج الاشتراك والمصروف والقبض وسجل الدفعات وشاشة الفاتورة |
| رسم كل الشاشات | كل شاشة وكل تبويب مالي وكل قسم إعدادات يُرسم بلا خطأ في الـ console |
| مسار الاستقبال الكامل | مشتركة ⇐ اشتراك بدفعة جزئية ⇐ وصل ⇐ قبض الباقي ⇐ إعادة طباعة ⇐ تجميد ⇐ حضور ⇐ بيع ⇐ إقفال الصندوق |
| حفظ وطباعة وصل | الطباعة لا تصنع مالاً، ومنع نافذتها لا يُفقد الدفعة ولا يُنشئ دفعة ثانية، وإعادة المحاولة تعطي الوصل نفسه |
| الاستقبال السريع | الكود + Enter (بالحرف وبالأرقام وبالأرقام العربية)، إنهاء التجميد اليوم واحتساب ما مضى وحده، وافتراضات التجديد وعدم مساس السجل القديم |
| ملاحظات الفريق | الحفظ بالكاتبة والتاريخ، رفض الفارغة وبلا مشتركة، التثبيت والتعديل والحذف |
| الفترات وتوزيعات الشركاء | لا توزيع قبل الإقفال، الربح يُثبَّت بالإقفال، التوزيع يقلّل السيولة ولا يقلّل الربح ولا يظهر مصروفاً، وكشف الشريكة |
| طرق رأس المال | النقد يدخل الدرج والتحويل والبطاقة لا، والاسترجاع ليس مصروفاً، والضخّ ليس إيراداً، وما لا تُعرف طريقته يُعزَل ويُصحَّح بفعل مسجَّل |
| دورة الشراء من المورّد | المستند وسطوره وترحيله: بضاعة تدخل، ومصروف واحد لا اثنان، وتسديد جزئي ومتبقٍّ، ومتوسط تكلفة مرجّح، وإلغاء يعكس الأثر ولا يمحو التاريخ، وكشف مورّد يجيب الأسئلة الثلاثة |
| تفصيل توزيعات الشركاء | مجموع الأشهر = المبلغ بالضبط، والتفصيل الصريح يُحفظ كما كُتب، والتعديل والإلغاء يُطابقان، والقديم بلا تفصيل يُقال عنه ذلك ولا يُخمَّن |
| الإقفال المُعان | المانع يمنع والتنبيه يُقَرّ ويُحفظ، والملخّص يقول ما يُقفَل عليه، وبعد الإقفال لا حركة إلا بتصحيح مُعلَّل مسجَّل |
| سلامة بيانات دورة الشراء | يتامى الشراء والتوزيع تُكسَر عمداً فيراها الفحص، والإصلاح يُغلق ثابت التفصيل ولا يحذف مستنداً صامتاً |
| الحصص المشمولة | المنح مع الاشتراك، الاستهلاك، إسقاطها بأرشفته كحدث ظاهر، وإعادتها بالاسترجاع |
| البحث العام | ثلاث نتائج للمجموعة، المشتركات أولاً، القفز بالكود ورقم الوصل، أفعال الصف، والتطبيع العربي |
| الشاشة الافتتاحية | الافتراضي والحفظ |
| المسار الحرج الكامل للمرحلة الأولى | الواحدة والثلاثون خطوة كاملة من إنشاء المشتركة إلى طباعة كشف الشريكة |
| المسار الحرج الكامل للمرحلة الثانية | ثلاثون خطوة: مشتركة ⇐ شراء من مورّد ⇐ رأس مال ⇐ إقفال ⇐ توزيع مفصَّل ⇐ نسخة ⇐ إعادة تحميل ⇐ استعادة ⇐ فحص سلامة، كل رقم مقارَن بلقطته |
| مساحة اللمس | لا زر تحت 32 بكسل على شاشة لمس، والمكتب لا يتغيّر |
| الهوية البصرية المتحرّكة | دورة حياة الشعار واللافتة ثابتَين ومتحرّكَين: رفع وعرض وإعادة تحميل ونسخة واستعادة واستبدال وإزالة، والكبير والنوع غير المدعوم يُرفضان، والطباعة تأخذ الإطار الثابت دائماً، والوسائط المكسورة لا تكسر الصفحة |
| الهوية المتحرّكة في الشاشة | الرفع من حقل الملف الحقيقي، والمعاينة تقول متحرّكة/ثابتة وحجماً وأبعاداً، وإعادة التحميل مرّتين لا تُنمّي الوسائط ولا النسخة |
| تشغيل الحركة الفعلي | إثباتٌ بمقارنة الإطارات لا بالوصف: لقطات متتابعة للعنصر نفسه في الأوضاع الثلاثة × جهازين (بتفضيل تقليل الحركة وبدونه)، مع ضبط مرجعي يثبت أن الملف يتحرّك في هذا المتصفح أصلاً، والطباعة ثابتة في كل وضع، والحركة تنجو من إعادة التحميل والاستعادة |
| تقليل الحركة | في سياقَي متصفح حقيقيَّين: التفضيل يجعل اللافتة والشعار ثابتين، وغيابه يُبقي الحركة، والطباعة ثابتة في الحالتين |
| التقييم الأولي عند التسجيل | السيناريوهان: تسجيل سريع بلا قياس ثم قياس لاحق، وتسجيل بقياسات أولية تدخل السجل نفسه — بلا قراءة مكرّرة، والخطأ يُكشف قبل إنشاء المشتركة |
| أثر الهوية المتحرّكة على الأداء | الرفع والإقلاع واللوحة والاستقبال وحجم النسخة، مقيسة قبل الصورة المتحرّكة وبعدها |
| النسخ الاحتياطي والاستعادة | كل الجداول في النسخة (ومنها المشتريات ودفعاتها وطرق رأس المال وتفصيل التوزيعات)، والاستعادة تُرجع السيولة والمتبقّي للموردين كما كانا، وإعادة التهيئة تفرّغ الجديد أيضاً |
| التقارير والتصديرات | بناء التقرير، فصل القيمة عن المقبوض، ومطابقته لسطور الإيراد |
| تكلفة المتجر المعروفة والمجهولة | صفر التكلفة يُقرأ «غير معروفة» لا «تساوي صفراً»، والربح يُحسب على ما تُعرف تكلفته وحده ويُعلن نقصه، وتسجيل التكلفة اليوم لا يخترع ربحاً لبيعةٍ تمّت أمس، وما تُعرف تكلفته + ما لا تُعرف = كل المبيعات حتى مع خصم الفاتورة |
| التصديرات المحاسبية | كل ورقة تطابق خدمتها رقماً برقم (مشتريات، دفعات موردين، مقبوضات، حركة نقد)، والخانة الرقمية الفارغة تبقى فارغة لا صفراً، ولا تسرّب ورقةٌ كلمة مرور |
| تدقيق الإصدار — الاستعادة الصادقة | نسخة تالفة فيها صفوف بلا معرّف: الرقم المُبلَّغ هو ما دخل فعلاً لا ما في الملف، والمستبعَد يُعدّ ويُذكر في سجل الأحداث، والنسخة السليمة لا تُبلّغ عن استبعاد |
| العرض الضيّق 360 | أضيق جهاز حديث شائع: لا تمرير أفقي للصفحة على ثلاث عشرة شاشة، ولا زرّ خارج الحافة، وشريط الصفحات يلتفّ داخل بطاقته |
| سطح المكتب — مصدر واحد وتهيئة | ملف الواجهة ومجلّد البناء متطابقان بايتاً ببايت ولا واجهة ثانية؛ والهوية والإصدار والمخطّط والأيقونة والنافذة وقائمة الصلاحيات (فارغة) وسياسة أمن المحتوى والمثبّت — كلّها تُقرأ من `tauri.conf.json` المشحون لا من وصف |
| سطح المكتب — الإقلاع ونافذة البدء | الواجهة تُعلن جاهزيتها مرّة واحدة بعد أن تُرسَم الشاشة، وتُعلنها كذلك حين يتعثّر الإقلاع — فلا تبقى نافذة البدء إلى الأبد؛ ولا مهلة اصطناعية قبل الإعلان |
| سطح المكتب — نافذة البدء وتقليل الحركة | في سياقَي متصفّح حقيقيَّين: بلا التفضيل يتحرّك الشريط ويظهر الشعار بحركة، ومعه شعارٌ ثابت وكلمةُ انتظار بلا حركة؛ وانتقال الشاشات يتوقّف كما كان |
| سطح المكتب — انتقال الشاشات والتركيز | العنوان والسياق والتمييز يتبعون الشاشة، والتركيز ينتقل إلى المحتوى الجديد ولا يُنتزع عند إعادة رسم الشاشة نفسها ولا من نافذة مفتوحة، والنوافذ تُوسَّط والطويلة منها لا تُقصّ |
| الهوية — صندوق الشعار ثابت | الأبعاد المرسومة تُقاس بالبكسل على 128 و512 و1024 و2400 وعلى نسبتين متطرّفتين: الشريط الجانبي وشاشة الدخول والمعاينة والطباعة لا يتغيّر أيٌّ منها، والصورة تُحتوى ولا تُمطّ، والمتحرّك كالثابت في الصندوق ويبقى ثابتاً في الطباعة |
| الفريق — ملف الموظفة يُقرأ بلا تعديل | كل حقل مسجَّل يُقرأ في الملف بلا حقل إدخال واحد، والتعديل يعود فيُقرأ؛ ودورٌ بلا صلاحية رواتب لا يرى راتباً، وبلا صلاحية تعديل لا يرى وثيقة تعريف |
| مركز الملفات — يجد ما أُنتج ويفتحه | يجد CSV وExcel وWord والنسخة اليدوية والتلقائية من المجلّدات لا من جدول، ويفتح بفئة واسم بلا مسار، والمحذوف من خارج التطبيق يُقال ناقصاً؛ وألف ملف لا تُقرأ كلّها ولا تُرسم كلّها |
| سطح المكتب — رسالة الحفظ تحمل فعلها | «تم حفظ ملف Excel» لا «تم الحفظ»، ومعها «فتح الملف» و«فتح المجلد» وموضعٌ عربيّ؛ والفعل يفتح ما حُفظ بالضبط، والفشل بلا فعل ولا ادّعاء |
| الطباعة — لا معاينة متصفّح في تطبيق ويندوز | Ctrl+P لا يصل إلى محرّك العرض بل إلى حوار النظام، وشاشةٌ بلا مستند تُجيب بالعربية؛ ودورة كاملة: طباعة، إلغاء، إعادة، فشل، تعافٍ — والعدّاد يعدّ الحوارات التي فُتحت فعلاً ولا يعدّ الفاشلة |
| المتصفح — لم يتغيّر شيء | لا جسر ولا مدخل لمركز الملفات، وCtrl+P لا يُعترض، والتصدير تنزيل، والطباعة في وعاء مستقلّ |
| البناء — هويّة يُفرَّق بها بين نسختين | معرّف البناء يُقرأ من الجسر ويُعرض تحت الإصدار، ولا بصمة ولا تاريخ يُخترعان في الواجهة، والمتصفح بلا هويّة ولا بديل؛ والنسخة متّفقة في المواضع الأربعة، والبصمة تُخبز وقت الترجمة، وسياسة نهايات الأسطر مكتوبة |
| خطّ البناء — بوّابات قبل المثبّت | الذاكرة المؤقّتة لا تحمل ناتج بناء، والبوّابات الأربع بترتيبها وكلّها قبل الرفع، والمثبّت يُثبَّت فعلاً ويُفحص ما ثُبّت، واسم الحزمة يحمل التشغيل والبصمة، ومثبّت واحد لا اثنان |
| سطح المكتب — الإقلاع والتخزين | الإقلاع خلف سياسة أمن المحتوى، والتخزين IndexedDB لا بديلاً احتياطياً، وأربعون جدولاً، ومخطّط 8، وكتابة وقراءة حقيقيتان، **ولا طلب شبكة خارجيّ واحد** |
| سطح المكتب — الطباعة | ستّة مستندات و`window.open` معطّلة: وعاء يُفتح، ومستند غير فارغ، و`print()` **مرة واحدة**، واتجاه RTL، وأنماط النظام، وقواعد `@page` — والوعاء مرسوم لا مخفيّ، وخارج الشاشة، ولا يتكدّس |
| سطح المكتب — النسخ الاحتياطي والهجرة | نادٍ كامل ⇐ نسخة ⇐ استعادة في سياق تخزين **نظيف**: الإجماليات الستّة وعدد سجلات الأربعين والسيولة والمستحقات كما كانت، والشعار المتحرّك عاد بإطاره، والمالكة والاستقبال دخلتا بكلمتَي المرور نفسيهما |
| سطح المكتب — الهوية المتحرّكة | الأوضاع الثلاثة × (تقليل الحركة مفعَّل/مطفأ) على وقت تشغيل سطح المكتب، والطباعة ثابتة في كل حال، والبقاء بعد إعادة التشغيل وعبر الاستعادة |
| سطح المكتب — الدخول والصلاحيات | تهيئة أول حساب ورمز الاسترجاع، والتلبيد لا النصّ، ورمز ردٍّ واحد لا يكشف وجود الحساب، ودور الاستقبال المحدود عند حدّ الفعل، والتعطيل، والاسترجاع، وإعادة التحميل لا تُخرج المستخدمة |
| سطح المكتب — بلا شبكة | كل طلب خارج المستند مقطوع، ثم يوم عمل كامل: مشتركة واشتراك ودفعة وحضور ومصروف ومخزون وبيع وتقرير ونسخة واستعادة وطباعة وتصدير — **وصفر طلب خارجيّ** |
| سطح المكتب — العربية والمسارات | أسماء عربية وبحث عربيّ، وأسماء ملفات التصدير عربية بلا فاصل مسار، واسم مستخدمة وكلمة مرور عربيّان، ولا اعتماد على مسار قرص أو مجلّد عمل |
| سطح المكتب — الأداء على قاعدة كبيرة | 132,235 سجلاً (10,000 مشتركة · 20,000 دفعة · 10,000 فاتورة · 22,000 حركة مخزون): أحد عشر قياساً **مقارَناً بالمتصفح** وبحدّ مطلق، والذاكرة |

## مجموعات سطح المكتب

تسع مجموعات في `tests/desktop.js` تفرض على متصفّح حقيقي **قيود وقت تشغيل
Tauri الثلاثة مجتمعة** بدل افتراض أنها لا تضرّ:

1. `window.open` تُرجع `null` — غلاف Tauri لا يسجّل معالج نوافذ جديدة، فيردّ
   wry الطلب بلا نافذة. كل ما بُني على نافذة منبثقة يسقط هناك صامتاً.
2. سياسة أمن المحتوى المكتوبة في `src-tauri/tauri.conf.json` تُرسَل ترويسةً
   فعلاً — تُقرأ من ملف التهيئة نفسه فلا يفترق الاختبار عمّا يُشحن.
3. الواجهة تُقدَّم من `app/` — مجلّد البناء الذي يشحنه المثبّت — لا من ملف
   الجذر.

وما **لا** تدّعيه: أنها WebView2. المحرّك هنا Chromium، وWebView2 مبنيّ
عليه أيضاً، لكنهما ليسا الشيء نفسه. ما لم يُقَس على ويندوز مكتوب في
`docs/QA-phase-4.md` §14 لا هنا.
